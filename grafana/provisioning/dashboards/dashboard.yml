apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus_ds

dashboards:
  - name: CrowdSec Overview
    orgId: 1
    folder: CrowdSec
    type: file
    options:
      path: /etc/grafana/dashboards/crowdsec-dashboard.json
